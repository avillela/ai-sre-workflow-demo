version: 0.0.1
title: Kubernetes Cluster Setup
description: Deploy a k8s KinD cluster, and install Argo CD
prompt: |
 Create and configure a Kubernetes KinD cluster with ArgoCD installed.

 Run the steps below and confirm when each command has been executed. Also list the output of each command:
 - Create a KinD cluster named "otel"
 - Install ArgoCD. All pods must be in "running" status before moving on to next step.
 - Print a "DONE" message upon completion of above tasks.

 Execute the above tasks. Summarize the results, and also return the results and status via final_output.

# response:
#   json_schema:
#     type: object
#     properties:
#       result:
#         type: string
#         description: "The main result of the task"
#       details:
#         type: array
#         items:
#           type: string
#         description: "Additional details of steps taken"
#     required:
#       - result
#       - status